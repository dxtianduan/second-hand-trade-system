# 基础骨架使用说明

> 这份文档对应「搭基础骨架」这一阶段。骨架已经写完并通过编译（16 个源文件，BUILD SUCCESS）。
> 下面每一步都告诉你：**这个文件是干什么的、你什么时候会用到它、怎么用**。

---

## 一、先做这一件事：改数据库密码

打开 `src/main/resources/application.yml`，找到第 24 行附近：

```yaml
spring:
  datasource:
    username: root
    password: 123456      # ← 改成你自己的 MySQL root 密码
```

**这一步不做，项目启动会报 `Access denied for user 'root'@'localhost'`。**
其他配置（端口 3306、库名 `second_hand_trade`）确认一下是否和你本机一致即可。

Redis 相关配置如果本机没装 Redis，启动时会打印连接失败警告，但**不影响启动**（因为还没写用到 Redis 的业务代码）。等做秒杀/缓存功能时再装也不迟。

---

## 二、骨架全景图

```
com.secondhand
├── SecondhandApplication          启动类
├── common
│   ├── result/Result              统一响应体 {code, msg, data}
│   ├── result/ErrorCode           错误码枚举（和接口文档 1.5 节一一对应）
│   └── exception/
│       ├── BusinessException      业务异常（Service 层抛这个）
│       └── GlobalExceptionHandler 全局异常兜底，把异常转成 Result
├── config/
│   ├── JwtProperties              JWT 配置绑定（读 application.yml）
│   ├── WebMvcConfig               注册拦截器 + 跨域
│   ├── MybatisPlusConfig          分页插件 + 防全表更新
│   └── MetaObjectHandlerConfig    create_time/update_time 自动填充
├── interceptor/LoginInterceptor    登录校验
├── util/
│   ├── JwtUtil                    JWT 生成/解析
│   └── UserHolder                 当前登录用户（ThreadLocal）
├── dto/UserDTO                    登录态里存的用户信息
├── entity/
│   ├── BaseEntity                 公共字段基类
│   └── User                       tb_user 实体
└── mapper/UserMapper              继承 BaseMapper<User>，零代码拥有 CRUD
```

---

## 三、每个文件怎么用（按你写代码时的顺序）

### 1. `Result` —— 所有 Controller 的返回值都包它

```java
@GetMapping("/{id}")
public Result<Goods> detail(@PathVariable Long id) {
    return Result.success(goodsService.getById(id));      // 成功带数据
}

@DeleteMapping("/{id}")
public Result<Void> delete(@PathVariable Long id) {
    goodsService.remove(id);
    return Result.success();                              // 成功无数据
}
```

约定：**HTTP 状态码永远是 200**，前端只看 `code` 是不是 200。

### 2. `BusinessException` —— Service 层发现业务不合法就抛它

```java
if (goods == null) {
    throw new BusinessException(ErrorCode.GOODS_NOT_FOUND);
}
if (!goods.getUserId().equals(userId)) {
    throw new BusinessException(ErrorCode.GOODS_NOT_OWNED, "这不是你发布的商品");
}
```

抛出去不用 try-catch，`GlobalExceptionHandler` 会自动接住并转成 `Result.error(...)`。

**好处**：业务代码里没有任何 try-catch 和错误码拼装，全是"检查 → 抛"。

### 3. `UserHolder` —— 在 Service 里拿当前登录用户

```java
Long userId = UserHolder.requireUserId();   // 没登录会抛 401
UserDTO user = UserHolder.requireUser();
UserHolder.requireAdmin();                  // 非管理员直接抛 403
```

**坑（重要）**：`UserHolder` 底层是 `ThreadLocal`，Tomcat 线程会被复用。
如果你写 `@Async` 异步方法或 `CompletableFuture`，子线程里**拿不到** `UserHolder`，
必须在提交任务前把 `userId` 取出来当参数传进去，不能在子线程里现取。

拦截器已经在 `preHandle` 开头和 `afterCompletion` 两处清理了，这块你不用担心。
但**别自己再往 `UserHolder` 里 set**，否则可能污染下一个请求。

### 4. `JwtUtil` —— 只在登录接口和拦截器里用

```java
// 登录成功后
String token = jwtUtil.createToken(userDTO);
return Result.success(new LoginVO(token, userDTO));

// 解析（一般不用你手动调，拦截器已经做了）
UserDTO user = jwtUtil.parseToken(token);
```

### 5. `UserMapper` —— 已经能用，直接注入

```java
@Autowired
private UserMapper userMapper;

// 这些方法全部免费拥有，不用写 SQL：
userMapper.selectById(1L);
userMapper.selectList(new LambdaQueryWrapper<User>().eq(User::getStatus, 1));
userMapper.selectOne(new LambdaQueryWrapper<User>().eq(User::getUsername, "zhangsan"));
userMapper.insert(user);
userMapper.updateById(user);
```

分页（要配合 `MybatisPlusConfig` 里的分页插件）：

```java
Page<User> page = new Page<>(1, 10);
userMapper.selectPage(page, new LambdaQueryWrapper<User>().eq(User::getStatus, 1));
long total = page.getTotal();    // 总条数，分页插件生效时才有值
List<User> records = page.getRecords();
```

### 6. `BaseEntity` + `MetaObjectHandlerConfig` —— 时间字段不用你管

新建 `Goods`/`Order` 等实体时，**只要 `extends BaseEntity`**，
`createTime` 和 `updateTime` 就会在 insert/update 时自动填好，你不用手写 `setCreateTime(new Date())`。

### 7. `MybatisPlusConfig` —— 已配好，知道有两件事被它管着即可

- **分页**：不注册分页插件的话，`page.getTotal()` 永远是 0，前端显示"共 0 条"但列表正常返回，非常迷惑。
- **防全表更新**：拦下没有 where 条件的 `update`/`delete`。这是**保护**——如果你写了
  `userMapper.update(entity, null)` 想改全表，会被直接拦下报错。这不是 bug，是防止你手滑。

---

## 四、验证骨架是否正常（建议现在就做一次）

项目已经能启动，但骨架没有业务接口，所以启动成功后只能看到日志、看不到页面。做个最小验证：

**第 1 步：启动项目**
在 IDEA 里运行 `SecondhandApplication`，看到这样的日志就是成功：

```
Tomcat started on port(s): 8080 (http)
Started SecondhandApplication in x.xxx seconds
```

**第 2 步：验证统一响应体**

浏览器访问：`http://localhost:8080/api/goods`

- 因为还没写商品接口 → 会掉到 `NoHandlerFoundException` → 返回类似：
  `{"code":404,"msg":"请求的接口不存在","data":null}`
- **关键点**：能看到这个 JSON 就说明 `Result` + `GlobalExceptionHandler` + 拦截器链路都通了。

**第 3 步：验证 401（可选）**

骨架里 `WebMvcConfig` 已经把 `/api/goods` 设成公开路径了，验证 401 需要临时把某个路径移出白名单。
这一步可以等写完认证模块再做，不必现在折腾。

---

## 五、接下来写代码的顺序

按这个顺序做，每一步都基于上一步的成果：

| 顺序 | 模块 | 产出 | 重点验证 |
|---|---|---|---|
| 1 | **认证模块** | 注册/登录/获取当前用户 | BCrypt 加密、JWT 签发、`UserHolder` 取值 |
| 2 | **商品模块** | 发布/列表/详情/下架 | 分页、ngram 全文搜索、权限校验（只能改自己的） |
| 3 | **订单模块** | 下单/取消/确认收货 | **乐观锁防超卖**（本项目最核心的知识点） |
| 4 | 支付模块 | 模拟支付、余额扣减 | 事务 + 幂等 |
| 5 | 退款 + 评价 | 退款申请、双向评价 | 状态机流转、商品 status 与订单 status 同步 |
| 6 | 收藏/留言/地址/聊天 | 剩余社交功能 | 聊天用 WebSocket |
| 7 | 管理端 | 用户封禁、商品审核、举报处理 | 管理员权限 |

**每个模块的标准动作**：
1. 写 `entity`（`extends BaseEntity`）
2. 写 `Mapper`（`extends BaseMapper<Xxx>`）
3. 写 `Service` 接口 + `ServiceImpl`
4. 写 `Controller`
5. 对照 `E:\second-hand-trade-system\docs\接口文档.md` 检查请求/响应字段是否一致

---

## 六、两个最容易踩的坑（提前说）

### 坑 1：商品 status 和订单 status 必须同步

这是本项目最容易出的 bug。规则：

| 订单状态 | 商品 status 应该是 |
|---|---|
| 1待付款 / 2待发货 / 3待收货 / 4已完成 / 6退款中 | 2（已售出） |
| 5已取消 / 7已退款 | 1（恢复在售） |

**下单成功那一刻就该把商品置为 2**，而不是等到付款。
否则两个人能同时拍下同一件商品，这是最典型的超卖场景。

### 坑 2：乐观锁判断成败要看影响行数

防超卖的正确写法（`tb_goods` 有 `version` 字段）：

```java
// 正确：条件更新，看影响行数
int rows = goodsMapper.update(null, new LambdaUpdateWrapper<Goods>()
        .eq(Goods::getId, goodsId)
        .eq(Goods::getStatus, 1)        // 必须还在售
        .set(Goods::getStatus, 2)
        .set(Goods::getVersion, version + 1));
if (rows == 0) {
    throw new BusinessException(ErrorCode.GOODS_SOLD_OUT);
}

// 错误：先查后改，两个线程都查到 status=1，都会下单成功
Goods goods = goodsMapper.selectById(goodsId);
if (goods.getStatus() == 1) {
    goods.setStatus(2);
    goodsMapper.updateById(goods);      // 挡不住并发
}
```

**"先查后改"在并发下必然失败**，这也是为什么 `MybatisPlusConfig` 里加了防全表更新——
它逼你把 where 条件写全。

---

## 七、需要你自己做的事（清单）

- [ ] 改 `application.yml` 里的 MySQL 密码
- [ ] 启动项目，确认看到 `Started SecondhandApplication`
- [ ] 访问 `http://localhost:8080/api/goods`，确认返回 JSON 而不是 HTML 错误页
- [ ] 开始写认证模块（下一步我带你做）

---

## 附：本机编译命令（IDEA 里不用管，命令行验证用）

本机没有独立 Maven，用 IDEA 内置的：

```bash
cd "C:/Users/lenovo/Desktop/second hand/secondhand"
MVN_HOME="D:/com.Java file/IntelliJ IDEA 2025.2/plugins/maven/lib/maven3"
"E:/JDK1.8/bin/java.exe" \
  -classpath "$MVN_HOME/boot/plexus-classworlds-2.8.0.jar" \
  "-Dclassworlds.conf=$MVN_HOME/bin/m2.conf" \
  "-Dmaven.home=$MVN_HOME" \
  "-Dmaven.multiModuleProjectDirectory=$(pwd)" \
  "-Dmaven.repo.local=C:/Users/lenovo/.m2/repository" \
  org.codehaus.plexus.classworlds.launcher.Launcher -o clean compile
```

注意必须带 `-Dmaven.multiModuleProjectDirectory`，否则报 `system property is not set`。
