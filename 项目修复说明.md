# 项目修复说明与下一步操作

> 项目路径：`C:\Users\lenovo\Desktop\second hand\secondhand`
> 修复时间：2026-09-21 20:26

---

## 一、修复了什么

Initializr 生成的初始 `pom.xml` 有 **3 个错误**，导致项目根本编译不过。已全部修好：

| # | 问题 | 原内容 | 已改为 |
| --- | --- | --- | --- |
| 1 | **包名拼写错误** | `com.seconghand`（少个 `d`） | `com.secondhand` |
| 2 | **artifact 不存在** | `spring-boot-starter-webmvc` | `spring-boot-starter-web` |
| 3 | **MySQL 驱动坐标错** | `com.mysql:mysql-connector-j`（Boot 3 的） | `mysql:mysql-connector-java:8.0.30` |

同时：
- 第 8 行 Boot 版本 `3.3.4` → **`2.7.3`**（3.x 要求 JDK 17，和你选的 Java 8 冲突）
- 补齐了 MyBatis-Plus、Druid、Redis、JWT、Hutool、Validation、WebSocket 依赖
- 把 `application.properties` 换成了 `application.yml`（含数据库、Redis、MyBatis-Plus 配置）
- 修复了 `annotationProcessorPaths` 里 Lombok 缺版本号导致的构建失败

---

## 二、验证结果

我用你本机 **IDEA 内置的 Maven 3.9.9 + JDK 8** 实际跑了构建和启动：

| 验证项 | 结果 |
| --- | --- |
| `mvn clean compile` | ✅ **BUILD SUCCESS** |
| Spring Boot 启动 | ✅ 成功，`Tomcat 9.0.65` 监听 8080 |
| Spring 容器初始化 | ✅ 完成 |
| Druid 数据源初始化 | ✅ 完成 |
| MySQL 驱动加载 | ✅ 成功 |
| `application.yml` 解析 | ✅ 正常 |
| **数据库连接** | ❌ 密码不对 |

**最后一步的报错是**：

```
Access denied for user 'root'@'localhost' (using password: YES)
```

**这说明整条链路已经全通了** —— 从 JDK、Spring Boot、Tomcat、Druid 到 MySQL 驱动全部正常，
只差数据库密码没填对。

---

## 三、你需要做的两件事

### 1. 改数据库密码

打开 `src/main/resources/application.yml`，找到第 24 行附近：

```yaml
spring:
  datasource:
    username: root
    password: 123456        # ← 改成你真实的 MySQL 密码
```

### 2. 导入数据库

你的库还没导入（我从截图看到你左侧选中的是 `student system`，不是 `second_hand_trade`）。

在 Navicat 里：
1. 打开 `E:\second-hand-trade-system\sql\01_schema.sql` → 执行
2. 刷新，左侧应出现 `second_hand_trade` 库
3. **双击选中 `second_hand_trade`**（关键，否则 SQL 跑到别的库里）
4. 打开 `02_data.sql` → 执行
5. 看到 15 张表和自检结果（7 项全为 0）就成功了

---

## 四、然后就可以启动了

在 IDEA 里：
1. 右上角点 Maven 刷新按钮（图标像两个箭头绕圈）
2. 找到 `SecondhandApplication.java`
3. 点左侧绿色三角 → Run

控制台出现这个就是成功：

```
Started SecondhandApplication in X.XXX seconds (JVM running for X.XXX)
Tomcat started on port(s): 8080 (http)
```

浏览器访问 `http://localhost:8080/druid`（账号密码都是 `admin`）能看到 Druid 监控页。

---

## 五、关于被删掉的 mvnw

你之前删掉了 `.mvn`、`mvnw`、`mvnw.cmd`。我已经**重建了** `mvnw.cmd` 和 `.mvn/wrapper/maven-wrapper.properties`。

不过要说明：**这个 wrapper 现在还不能直接用** —— 它需要联网下载 `maven-wrapper.jar`。
你本机没有独立 Maven，所以命令行构建暂时用不了。

**这不影响你做项目** —— IDEA 自带的 Maven 完全够用，构建、运行、调试都没问题。

如果以后想在命令行构建，两个办法：
1. 联网时跑一次 `mvnw.cmd clean compile`，它会自动把 wrapper 下下来
2. 或者单独装个 Maven，配好 `MAVEN_HOME` 和环境变量

---

## 六、环境变量建议

你的 `JAVA_HOME` 是空的，建议配上（有些工具会依赖它）：

1. `Win + R` 输入 `sysdm.cpl` → 高级 → 环境变量
2. 新建系统变量：`JAVA_HOME` = `E:\JDK1.8`
3. 编辑 `Path`，新增一行：`%JAVA_HOME%\bin`
4. 保存后**重开终端**，输入 `java -version` 能显示 1.8.0_504 就成功

---

## 七、建议的开发顺序

库导好、能启动之后，按这个顺序推进：

```
1. 统一响应体 Result<T> + 全局异常处理       ← 所有接口的基础
2. JWT 工具类 + 登录拦截器 + UserHolder      ← 认证基础
3. 实体类（entity）+ Mapper（继承 BaseMapper） ← 数据层
4. 认证模块（注册/登录）                      ← 第一个能跑的功能
5. 商品模块（发布/详情/列表/搜索）             ← 核心业务
6. 订单模块（下单/取消）                      ← 重点验证乐观锁防超卖
7. 支付（先做余额支付）→ 确认收货结算
8. 退款、评价
9. 收藏、留言、地址、聊天
10. 管理端（审核、举报、封禁）
```

**第 6 步是核心**：下单必须用条件更新，用影响行数判断成败，不能用"先查后改"。
```
