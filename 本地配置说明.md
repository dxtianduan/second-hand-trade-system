# 本地配置说明

本项目把**敏感配置**（数据库密码、JWT 密钥、Druid 监控账号）全部改为**环境变量注入**，
仓库里的 `application.yml` 只保留占位符，不含任何真实密码。

> 为什么这样做：GitHub 上有大量自动化爬虫专门扫描公开仓库里的
> `application.yml` / `application.properties`，抓取其中的数据库密码和密钥。
> 一旦真实密码进入 git 历史，即使之后删掉也仍可从历史记录中翻出来。

---

## 一、需要设置的环境变量

| 变量名 | 说明 | 示例值 |
| --- | --- | --- |
| `DB_USERNAME` | MySQL 用户名 | `root` |
| `DB_PASSWORD` | MySQL 密码 | 你自己的密码 |
| `JWT_SECRET` | JWT 签名密钥 | 随机长字符串 |
| `DRUID_USERNAME` | Druid 监控页账号（可选） | `admin` |
| `DRUID_PASSWORD` | Druid 监控页密码（可选） | `admin` |

> `DB_USERNAME` 默认值为 `root`，不设也能用。
> `DRUID_*` 不设时默认 `admin` / `admin`，仅在本地调试用。

---

## 二、怎么设置

### 方式 A：IntelliJ IDEA（推荐）

1. 顶部菜单 **Run → Edit Configurations...**
2. 左侧选中 `SecondhandApplication`
3. 找到 **Environment variables** 一栏，点右侧图标展开
4. 填入（用分号分隔）：

```
DB_USERNAME=root;DB_PASSWORD=你的真实密码;JWT_SECRET=随便一串长随机字符
```

5. 点 **OK** 保存，然后正常启动

### 方式 B：命令行启动

**Git Bash：**

```bash
export DB_USERNAME=root
export DB_PASSWORD='你的真实密码'
export JWT_SECRET=$(openssl rand -hex 32)
./mvnw spring-boot:run
```

**PowerShell：**

```powershell
$env:DB_USERNAME="root"
$env:DB_PASSWORD="你的真实密码"
$env:JWT_SECRET="随便一串长随机字符"
.\mvnw.cmd spring-boot:run
```

**CMD：**

```cmd
set DB_USERNAME=root
set DB_PASSWORD=你的真实密码
set JWT_SECRET=随便一串长随机字符
mvnw.cmd spring-boot:run
```

### 方式 C：生成一个像样的 JWT 密钥

```bash
openssl rand -hex 32
```

把输出结果作为 `JWT_SECRET` 的值即可。

---

## 三、启动前检查清单

- [ ] MySQL 已启动，`second_hand_trade` 库已导入（见 SQL 脚本）
- [ ] Redis 已启动（`localhost:6379`）
- [ ] `DB_PASSWORD` 环境变量已设置为真实密码
- [ ] 端口 `8080` 未被占用

---

## 四、如果启动报错

| 报错 | 原因 | 解决 |
| --- | --- | --- |
| `Access denied for user 'root'@'localhost'` | `DB_PASSWORD` 没设或设错 | 检查环境变量是否生效，注意密码里的特殊字符要加引号 |
| `Public Key Retrieval is not allowed` | 连接串缺参数 | 确认 url 里带 `allowPublicKeyRetrieval=true` |
| `Communications link failure` | MySQL 没启动 | 启动 MySQL 服务 |
| `Unable to connect to Redis` | Redis 没启动 | 启动 Redis 服务 |
| `JWT signature does not match` | 换了 `JWT_SECRET` | 旧 token 失效属正常，重新登录即可 |
